const defaultState = {
    posts: [],
    postCount: 0,
    post: {}
}

const site = (state = defaultState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default site;